baseURL = "https://sarcastic.news/"
languageCode = "en-us"
title = "Sarcastic News"
theme = "mainroad"
paginate = "10" # Number of posts per page
googleAnalytics = "G-MRRZYLYQC7"

[Author] # Used in authorbox
  name = "John Doe"
  bio = "John Doe's true identity is unknown. Maybe he is a successful blogger or writer. Nobody knows it."
  avatar = "img/avatar.png"

[Params]
  description = "Sarcastic News!  All the hot gossip from around the world" # Site description. Used in meta description
  copyright = "XYZ Consulting LLC" # Footer copyright holder, otherwise will use site title
  opengraph = true # Enable OpenGraph if true
  schema = true # Enable Schema
  twitter_cards = true # Enable Twitter Cards if true
  readmore = true # Show "Read more" button in list if true 
  authorbox = true # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  post_meta = [ "date", "categories"] # Order of post meta information
  mainSections = ["post", "news", "US", "World", "Technology", "Entertainment", "Other", "Politics"] # Specify section pages to show on home page and the "Recent articles" widget
  subSections = ["World","Technology","Entertainment","Other", "Politics"]
  dateformat = "2006-01-02" # Change the format of dates
  mathjax = false # Enable MathJax
  mathjaxPath = "https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.6/MathJax.js" # Specify MathJax path
  mathjaxConfig = "TeX-AMS-MML_HTMLorMML" # Specify MathJax config
  googleFontsLink = "https://fonts.googleapis.com/css?family=Open+Sans:400,400i,700" # Load Google Fonts
  # customCSS = ["css/custom.css"] # Include custom CSS files
  # customJS = ["js/custom.js"] # Include custom JS files

[Params.style.vars]
  # highlightColor = "#e22d30" # Override highlight color
  highlightColor = "#3ba5a9"

  # Override font-family sets
  # Take care of different quotes OR escaping symbols in these params if necessary
  fontFamilyPrimary = "'Open Sans', Helvetica, Arial, sans-serif"
  # Secondary font-family set responsible for pre, code, kbd, and samp tags font
  fontFamilySecondary = "SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace"

[Params.logo]
  image = "img/Sarcastic-news-logo_clear.png" # Logo image. Path relative to "static"
  title = "Sarcastic News" # Logo title, otherwise will use site title
  subtitle = "All the hot gossip from around the world" # Logo subtitle

[Params.thumbnail]
  visibility = ["list", "post"] # Control thumbnail visibility

[Params.sidebar]
  home = "right" # Configure layout for home page
  list = "left"  # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories",  "social"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  skip_num = 5 # Set the number of articles in the "Recent articles" widget to skip
  categories_counter = false # Enable counter for each category in "Categories" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget

[Params.widgets.social]
  cached = false # activate cache if true
  # Enable parts of social widget
  twitter = "username"
  instagram = "username"
  email = "info@sarcastic.news"

[[Params.widgets.social.custom]]
  title = "Sarcastic News"
  url = "https://sarcastic.news"

[Params.widgets.search]
  cached = true # activate cache if true
  url = "https://google.com/search"
  [Params.widgets.search.input]
    name = "sitesearch"
    pre = ""